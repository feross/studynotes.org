extends layouts/main-sidebar

append sidebar
  include includes/ads/adsense-rect
  include includes/countdown
  include includes/ads/adsense-rect
  h2 #{ course.name } #{ notetype.name } Stats
  ul
    li #{ util.addCommas(notetype.hits) } views
    li.online

block content
  include includes/breadcrumbs
  h1= notetype.name
  include includes/toolbar

  include includes/ads/adsense-inline-rect

  p!= notetype.desc || notetype.shortDesc

  if (locals.notes)
    ul.listing
      each note in notes
        li: a(href='#{note.url}')= note.name
