header.header
  block header
    ul.left
      li.home
        a(href='/', title='Go to the front page of StudyNotes', class='home')
          i.fa.fa-home.hide-phone.hide-tablet
          span Study Notes

    //- "off" class gets removed after search bar is positioned in JS
    .search.off.hide-phone
      i.fa.fa-search
      form(action='/search/', method='GET')
        input(autocorrect='off', autocomplete='off', spellcheck='false', name='q', placeholder='Search for AP Courses, Topics, and more...', type='text', value='#{locals.searchTerm}', autofocus=(searchFocus === false ? undefined : 'autofocus'))

    .autocomplete.off
      a.result(href='#')
        span.name
        span.desc

    ul.right
      li: a.courses(href='/courses/', title='Browse all AP study notes')
        i.fa.fa-chevron-down.hide-phone
        span AP Notes
      li: a.colleges(href='/essays/', title='Browse all admissions essays')
        i.fa.fa-chevron-down.hide-phone
        span College
        span.hide-phone Essays

      if (locals.currentUser)
        li.hide-phone: a(href='#{currentUser.url}', title='Manage your account')
          img(src='#{ currentUser.gravatarBlank }', width=21, height=21).hide-phone.hide-tablet
          i.fa.fa-user.hide-tablet
          span= currentUser.firstName
        li.hide-phone: a.logout(href='#logout', title='Logout of StudyNotes')
          i.fa.fa-sign-out.hide-tablet
          span Logout
      else
        li.hide-phone: a(href='/signup/', title='Create a StudyNotes Account')
          i.fa.fa-check-square-o.hide-tablet
          span Sign Up
        - var loginUrl = '/login/' + (locals.url ? '?returnTo=' + url : '')
        li.hide-phone: a(href=loginUrl, title='Login to StudyNotes')
          i.fa.fa-sign-in.hide-tablet
          span Log In
