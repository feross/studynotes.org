extends layouts/main-sidebar

append sidebar
  include includes/ads/adsense-rect
  h3 User Stats
  p <strong>#{ util.addCommas(theUser.hits) } people</strong> have viewed #{ theUser.name }'s profile.

block content
  include includes/breadcrumbs
  h1 Notes by #{ theUser.name }

  include includes/toolbar

  p Nothing to see here yet

