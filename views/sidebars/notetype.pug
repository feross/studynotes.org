include ../ads/adsense-sidebar
include ../includes/countdown
include ../ads/adsense-sidebar
h2 #{ course.shortName } #{ notetype.name }
ul
  li #{ util.addCommas(courseNotetype.hits) } views
  li.online
