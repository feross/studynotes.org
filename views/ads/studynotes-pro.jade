if locals.ads
  if college
    - var proUrl = '/pro/' + college.id + '/?referringEssay=' + encodeURIComponent(url)
  else
    - var proUrl = '/pro/?referringEssay=' + encodeURIComponent(url)

  h2 Get Study Notes Pro
  p For just $#{ (config.proPrice / 100) }, get <strong>lifetime access to #{ essayCount || essays.length }&nbsp;college essays</strong> that worked at #{ college ? college.shortName : 'Stanford, Harvard, Princeton,' } and more. <a href="#{ proUrl }">Learn&nbsp;more.</a>
  .center: a.btn.large.red(href=proUrl) Buy Now
